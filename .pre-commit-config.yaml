---
exclude: ^(public|themes|node_modules|pnpm-lock.yaml)
repos:
- repo: https://github.com/pre-commit/pre-commit-hooks
  rev: v5.0.0    # Use the latest stable version
  hooks:
  - id: trailing-whitespace
  - id: end-of-file-fixer
  - id: check-yaml
  - id: check-added-large-files
    args: [--maxkb=500]
  - id: pretty-format-json
    args: [--autofix]

- repo: https://github.com/alessandrojcm/commitlint-pre-commit-hook
  rev: v9.22.0
  hooks:
  - id: commitlint
    stages: [commit-msg]
    additional_dependencies: ['@commitlint/config-conventional']

- repo: https://github.com/adrienverge/yamllint.git
  rev: v1.21.0
  hooks:
  - id: yamllint
    args: [--format, parsable, --strict, -d, '{extends: default, rules: {line-length: disable, indentation: disable, truthy: disable}}']

- repo: https://github.com/jumanjihouse/pre-commit-hook-yamlfmt
  rev: 0.2.1
  hooks:
  - id: yamlfmt
    args: [--mapping, '2', --sequence, '2', --offset, '0']

- repo: https://github.com/psf/black
  rev: 23.1.0    # Use the latest stable version
  hooks:
  - id: black

  # - repo: https://github.com/oxc-project/mirrors-oxlint
  #   rev: v1.41.0
  #   hooks:
  #   - id: oxlint
  #     name: web oxlint
  #     files: ^web/.*\.(vue|ts|tsx|js|jsx)$
